<div class="container">
	<header class="d-flex flex-wrap align-items-center justify-content-center justify-content-md-between py-3 mb-4 border-bottom">
		<div class="col-md-3 mb-2 mb-md-0">
			<a href="/" class="d-inline-flex link-body-emphasis text-decoration-none">
				<img src="{{ asset('assets/logo/EcoRide_Logo2.png')}}" height="40">
			</a>
		</div>

		<ul class="nav col-12 col-md-auto mb-2 justify-content-center mb-md-0">
			<li>
				<a href="{{ path('app_home') }}" class="nav-link px-2 link-secondary">Accueil</a>
			</li>
			<li>
				<a href="{{ path('app_covoiturages') }}" class="nav-link px-2">Covoiturages</a>
			</li>
			<li>
				<a href="{{ path('app_covoiturages_v2') }}" class="nav-link px-2">Covoiturages V2</a>
			</li>
			<li>
				<a href="{{ path('app_contact') }}" class="nav-link px-2">Contact</a>
			</li>
			{% if is_granted('ROLE_ADMIN') %}
				<li>
					<a href="{{ path('admin') }}" class="nav-link px-2">Admin</a>
				</li>
			{% endif %}
			{% if is_granted('ROLE_EMPLOYE') %}
				<li>
					<a href="{{ path('admin_employe') }}" class="nav-link px-2">Espace Employé</a>
				</li>
			{% endif %}
		</ul>

		<div class="col-md-4 text-end">
			{% if app.user %}
				<a href="#" class="btn btn-outline-primary me-2">
					{{ app.user.credit }}
					crédits</a>
				<a href="{{ path('app_mon_espace') }}" class="btn btn-outline-primary me-2">Mon Espace</a>
				<a href="{{ path('app_logout') }}" class="btn btn-primary">Logout</a>
			{% else %}
				<a href="{{ path('app_login') }}" class="btn btn-outline-primary me-2">Login</a>
				<a href="{{ path('app_register') }}" class="btn btn-primary">Sign-up</a>
			{% endif %}
		</div>
	</header>
</div>
